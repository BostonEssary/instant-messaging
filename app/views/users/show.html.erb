<%= turbo_frame_tag 'users' do %>
  <%= @user.username%>

  <%= image_tag @user.avatar.representation(resize_to_limit: [100, 100]) %>

  <%= button_to "Create Chat", chats_path(user_ids: [@user.id]), method: :post, data: { turbo_frame: "_top" }  %>
<% end %>